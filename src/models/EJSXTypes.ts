/**
 * Those are the .tsx elements from src/components/Inputs
 */
export enum EJSXTypes {
    BASIC = 1,
    BUTTONGROUP = 2,
    MAP = 3,
    TEXTFIELD = 4,
    SELECT = 5,
    IMAGE = 6,
    SLIDER = 7
}